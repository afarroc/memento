# Development / optional tooling for MementoBloom
# Not needed for core runtime; useful for local workflows and legacy scripts.

# Observed in venv (functional, not strict runtime pins)
redis>=8.0.0
PyMySQL>=1.2.0
openpyxl>=3.1.5
pypdf>=6.13.2
et_xmlfile>=2.0.0
