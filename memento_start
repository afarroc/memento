#!/bin/bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export MEMENTO_WORKSPACE="${MEMENTO_WORKSPACE:-$SCRIPT_DIR}"
cd "$SCRIPT_DIR"
exec python3 tools/session_start.py "$@"
